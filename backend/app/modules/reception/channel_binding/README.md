# channel_binding 模块

用于承接“微信接入（个人微信）”的扫码身份绑定能力。

当前只负责：
- 生成平台侧扫码绑定会话
- 记录租户当前绑定的接入身份
- 提供公开绑定页所需的查询与确认接口

当前不负责：
- 真实个人微信消息收发
- 替代现有 `wecom` webhook 出入站链路

这样做的目标是先把“扫码绑定入口”和“平台侧绑定状态”做成可视化、可轮询、可替换的独立能力，后续底层若更换实际微信接入实现，这一层不需要推翻。
