# reception

接待层模块，位于“渠道 -> 安全监听 -> 接待 Agent -> 安全监听 -> 渠道输出”的链路中间。

职责：

- 接收微信、钉钉等渠道消息
- 在输入和输出侧做实时安全监听、阻断、审计
- 将会话交给接待 Agent（当前可接 Hermes，后续必须支持替换）
- 将回复回传到对应渠道

约束：

- 安全监听只监听、判定、阻断、审计，不负责回复
- 接待节点绑定 `agent_id`，不能在流程层写死 Hermes 名称
- 接待层内部多 Agent 协作需要通过 NATS 总线衔接
