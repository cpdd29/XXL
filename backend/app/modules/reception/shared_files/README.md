# 接待附件中转

该模块用于把外接接待 Agent（如 Hermes）返回的本地附件清单，安全地接入平台并提供下载链接。

## 核心职责

- 只接受白名单类型文件（`md/pdf/docx/xlsx/图片/视频`）。
- 只允许读取 Hermes 接待工件目录，防止任意路径读取。
- 拷贝到平台自有存储目录后再对外提供下载入口。
- 生成短期/长期可访问的下载令牌（当前版本为运行期令牌）。

## 边界

- 不负责渠道发送文件二进制；当前只输出下载链接，由渠道文本回传。
- 不负责文件内容审核；内容安全继续由接待安全监听策略负责。

