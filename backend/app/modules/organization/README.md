# organization

组织模块。

这里负责租户、人员画像、用户资料、组织范围隔离、记忆沉淀等数据，是前台接待和后台调度的组织基础层。

- `application/`：组织业务服务，包括用户、画像、租户、记忆治理。
- `schemas/`：组织模块 API 与内部数据结构。
- `memory_store/`：组织模块自己的中长期记忆存储实现，承载 SQLite / Chroma 的具体读写适配。
