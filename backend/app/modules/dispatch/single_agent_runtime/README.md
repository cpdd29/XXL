# dispatch/single_agent_runtime

单 Agent 执行运行时目录。

适用于任务量不大、链路简单、希望先把单 Agent 方案走通的场景。

当前已纳入：

- `free_workflow_service.py`：单 Agent 自由工作流执行
- `agent_execution_service.py`：单 Agent 执行编排与模型调用
- `agent_execution_worker_service.py`：单 Agent 执行 worker
